# Back action

Tapping the Moment bottom control bar sends a back action to the current app. When the
first tap does not drag the bar, the system waits for the double-tap decision to finish
before executing a single back action.

If the Moment is not focused, the first tap activates the window instead of immediately
navigating back in the app. A second tap executes the back action.
